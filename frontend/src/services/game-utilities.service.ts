import type {BaseObject} from "@/models/base-object.ts";

// public static gameObjets: BaseObject[] = [];

export function getObjectAtPosition(x: number, y: number): BaseObject | undefined {
    return undefined;
}